type Query {
  players: [Player!]!
  gameStatus: GameStatus!
}

type Mutation {
  createUser(name: String!): Player!
}

type Player {
  id: ID!
  name: String!
  team: Team!
}

enum Team {
  RUST
  PYTHON
  JS
}

enum GameStatus {
  STARTED
  STOPPED
}
